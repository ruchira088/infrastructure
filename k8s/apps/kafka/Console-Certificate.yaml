apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kafka-console-certificate
  namespace: kafka
spec:
  secretName: kafka-console-tls-secret
  issuerRef:
    kind: ClusterIssuer
    name: lets-encrypt
  dnsNames:
    - kafka.home.ruchij.com
