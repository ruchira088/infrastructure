apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: headlamp-certificate
  namespace: kube-system
spec:
  secretName: headlamp-tls-secret
  issuerRef:
    kind: ClusterIssuer
    name: lets-encrypt
  dnsNames:
    - headlamp.home.ruchij.com